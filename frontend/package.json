{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && cdk deploy --all --require-approval never && aws cloudfront create-invalidation --distribution-id EE079INK46INE --paths '/*'"
  },
  "dependencies": {
    "@mapbox/polyline": "^1.2.1",
    "leaflet": "^1.9.4",
    "mobile-detect": "^1.4.5",
    "querystring": "^0.2.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-leaflet": "^5.0.0",
    "react-router-dom": "^7.5.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@types/mapbox__polyline": "^1.0.5",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.1",
    "aws-cdk": "^2.1016.0",
    "aws-cdk-lib": "^2.196.0",
    "constructs": "^10.4.2",
    "typescript": "^4.1.2"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
